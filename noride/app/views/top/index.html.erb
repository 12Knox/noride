<div class="contents_wrap">
  <div class="left_contents clearifx">
    <nav>
      <ul>
        <% if user_signed_in? %>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
        <li><%= link_to "User Login", "users/sign_in" %></li>
        <li><%= link_to "Developer Login", "/developers/sign_in" %></li>
        <% end %>
      </ul>
    </nav>
  </div>
  <div class="right_contents clearfix">
  <% if user_signed_in? %>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
    <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <div class="each_rides">
    <p class="each_rides_title">My ride!!</p>
  </div>
  <% else %>
  <div class="not_login_view">
    <p class="not_login_view_title">This is Noride Website.</p>
    <p></p>
  </div>
  </div>
  <% end %>
  </div>
</div>